name: cgapp
description: Central Glass App
publish_to: "none"

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.6.0
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.2.0

  auto_route: ^7.9.2
  auto_route_generator: ^7.3.2

  mobile_scanner: ^3.2.0

  shared:
    path: ../shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.9

flutter:
  uses-material-design: true
